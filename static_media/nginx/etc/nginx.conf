events {
    worker_connections 1024;
}

http {
    include mime.types;
    sendfile on;

    server {
        root /var/www;
        
        listen 8080;
        listen [::]:8080;

        server_name localhost;
        server_tokens off;

        gzip_static on;
    }
}